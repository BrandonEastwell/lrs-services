export default [{
    env: {
        node: true,  // Adds 'process', 'module', etc.
        browser: true, // Allows 'window' and 'document' to be recognized
    },
    rules: {
            // Your custom rules
    },
}]
